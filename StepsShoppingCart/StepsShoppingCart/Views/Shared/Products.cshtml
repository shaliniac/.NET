@model IEnumerable<StepsShoppingCart.Models.ProductViewModel>

@if (Model != null && Model.Any())
{

    <table>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfProductsAvailable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductPrice)
            </th>
            <th></th>
        </tr>

        @foreach (var product in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => employee.ProductName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => employee.NumberOfProductsAvailable)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => employee.ProductPrice)
                </td>
            </tr>
        }
    </table>
}

else
{
    @Html.Label("No Results found")
}